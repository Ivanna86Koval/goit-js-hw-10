.container {
  max-width: 1280px;
  margin: 0 auto;
  padding: 0 15px;
}

.breed-select {
  margin-bottom: 30px;
}

.loader {
  width: 10px;
  height: 10px;
  border-radius: 50%;
  display: none;
  margin: 15px auto;
  position: relative;
  color: #000;
  left: -100px;
  box-sizing: border-box;
  animation: shadowRolling 2s linear infinite;
}
